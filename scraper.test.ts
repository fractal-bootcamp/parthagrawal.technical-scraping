import { expect, describe, it } from 'vitest'
import { fetchHtml } from './scraper'

describe('function tests', () => {
    it('should fetch HTML', async () => {
        const response = fetchHtml('test.html')
        // expect()
    })
})